<h1>
  <% if user_signed_in? %>
    <small><%= link_to 'Ask question', new_question_path %></small>
  <% end %>
</h1>
<%= render @questions %>
<div class='raw'>
  <div class='col-md-12'>
    <%= paginate @questions %>
  </div>
</div>