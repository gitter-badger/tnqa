<% if @answer.errors.present? %>
    <% @answer.errors.full_messages.each do |message| %>
        $('.answer_errors').html('<%= j message %>');
    <% end %>
<% else %>
    $('.answers').html('<%= j render 'questions/answers' %>');
    $('#new_answer').each (function(){this.reset();
    });
<% end %>